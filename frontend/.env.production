# Atlas backend URL baked into the hosted bundle.
#
# Each user runs their own `atlas start` on their own machine; the hosted UI
# talks to THEIR localhost, not a shared server. Change this only if you want
# the hosted UI to target a different port.
VITE_API_BASE=http://localhost:8765
